{% extends 'base.html.twig' %}

{% block title %}Modifier l'employé {{ employee.name }}{% endblock %}

{% block body %}
<h1>Modifier l'employé</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}

    <button class="btn btn-primary mt-3">Enregistrer</button>
{{ form_end(form) }}

<a href="{{ path('app_employee_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
{% endblock %}
