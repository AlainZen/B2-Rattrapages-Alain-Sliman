{% extends 'base.html.twig' %}

{% block title %}Nouveau restaurant - Mongoo{% endblock %}

{% block body %}
<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
    <h1 class="h2"><i class="fas fa-plus-circle me-2"></i>Créer un nouveau restaurant</h1>
    <div class="btn-toolbar mb-2 mb-md-0">
        <a href="{{ path('app_restaurant_index') }}" class="btn btn-sm btn-secondary">
            <i class="fas fa-arrow-left me-1"></i> Retour à la liste
        </a>
    </div>
</div>

<div class="card">
    <div class="card-body">
        {{ include('restaurant/_form.html.twig') }}
    </div>
</div>
{% endblock %}
